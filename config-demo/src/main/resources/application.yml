# app:
#    name: My Application 2

test: Just testing something here real quickly

my.list.values: One, Two, Three

dbvalues: "{connectionString: 'http://__', userName: 'foo', password: '123'}"

db:
  connection: "{connectionString: 'http://__', userName: 'foo', password: '123'}"
  host: 127.0.0.1
  port: 1200

management.endpoints.web.exposure.include: "*"     #Just for testing. Do not do this in a production system!!!

# spring.profiles.active: dev                     # java -jar config-demo-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev

spring.cloud.config.uri: http://localhost:8888